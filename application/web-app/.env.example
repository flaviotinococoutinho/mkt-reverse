# API base URL exposed to the browser.
# Recommended: keep it relative so it works behind nginx and through Vite proxy in dev.
VITE_API_URL=/api/v1

# Vite dev-server proxy target (server-side only).
VITE_API_TARGET=http://localhost:8081
